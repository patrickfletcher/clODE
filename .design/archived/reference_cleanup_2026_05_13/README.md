# Reference Cleanup 2026-05-13

Purpose: hold historical spillover moved out of `.design/reference/` during the reference-note cleanup pass.
Read when: a live reference note points here for prior rationale, bug archaeology, or broader alternatives that are no longer part of the current active guidance.
Update when: one of these archived notes needs a signpost correction.

Historical only. The current source of truth remains the root `.design` docs plus the still-live notes under `.design/reference/`.

## Notes

- `backend_strategy_audit.md`: historical closeout note for the PyOpenCL-only backend decision.
- `continuation_timebase_background.md`: debugging-level continuation analysis and the alternative observer-time model discussion from the landed fix.
- `semantic_layout_background.md`: broader peer-comparison and package-layout option analysis removed from the slimmed live semantic-layout note.
